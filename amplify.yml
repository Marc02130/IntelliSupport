version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 18.19.0  # Install the required Node.js version
        - nvm use 18.19.0      # Use the installed version
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build    # Changed from 'dist' to 'build'
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
  customHeaders:
    - pattern: '**/*'
      headers:
        - key: 'Cache-Control'
          value: 'no-cache'
  redirects:
    - source: '</^[^.]+$|\\.(?!(css|gif|ico|jpg|js|png|txt|svg|woff|ttf|map|json)$)([^.]+$)/>'
      target: '/index.html'
      status: '200'